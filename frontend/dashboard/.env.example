# OpenOffensive dashboard — example environment. Copy to `.env` (git-ignored).
# The dashboard authenticates with a session cookie, so NO secrets belong here.

# Where the Vite dev server proxies /api during `npm run dev`.
VITE_PROXY_TARGET=http://localhost:8000

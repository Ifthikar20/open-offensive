# OpenOffensive — landing page

A small, static Astro site for **openoffensive.ai**. No secrets, no build-time keys.

```bash
npm install
npm run dev       # local dev server
npm run build     # -> dist/ (static, deploy to GitHub Pages / any static host)
```
